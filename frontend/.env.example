# Local dev: leave unset to use Vite proxy (/api → http://127.0.0.1:8000).
# Production / remote API: origin only, no trailing slash.
#
# Example (Render):
# VITE_API_BASE_URL=https://meeting-intelligence-api-e2ai.onrender.com
